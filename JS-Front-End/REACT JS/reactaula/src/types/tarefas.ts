// criando uma interface, uma variavel que a gente coloca os tipos da variavel dentro da tarefa
export interface ITarefa{
    tarefa: string,
    tempo: string,
    selecionado: boolean,
    completado: boolean,
    id: string
}
